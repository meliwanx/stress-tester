# 服务器说明

本文档记录“生图压测平台”的服务器对应关系，避免和其他压测工具或服务器混淆。

## 正确服务器

- 服务器 IP：`116.142.250.54`
- 对外访问：`http://116.142.250.54:8000`
- 服务端口：`8000`
- 线上代码目录：`/opt/stress_test`
- 运行命令特征：`/opt/conda/bin/python main.py`
- 前台供应商提交页：`http://116.142.250.54:8000/`
- 管理后台：`http://116.142.250.54:8000/admin`

## 本地对应目录

- 正确本地目录：`/Users/liwanx/Documents/Python/生图压测平台`
- 不要混用目录：`/Users/liwanx/Documents/Python/stress-tester`

`stress-tester` 是单独的“API 图片生成压测工具”，不是 116 上的生图压测平台生产代码。

## 已核对的线上状态

核对时间：`2026-06-14`

116 服务器 `8000` 端口当前由 `/opt/stress_test` 下的 `main.py` 提供服务。线上目录里存在历史包：

- `stress_test_v2.tar.gz`
- `stress_test_v3.tar.gz`
- `stress_test_v4.tar.gz`

核对时线上正在运行的文件与 `stress_test_v2.tar.gz` 一致，属于旧版；本地 `/Users/liwanx/Documents/Python/生图压测平台` 是 `v4` 同源后的更新版，并额外包含管理员手动创建任务等新功能。

## 部署前检查

部署到 116 前必须先做这些检查：

1. 确认目标仍是 `116.142.250.54:8000`。
2. 确认线上代码目录仍是 `/opt/stress_test`。
3. 备份 `/opt/stress_test` 整个目录。
4. 单独备份 `/opt/stress_test/data.db`。
5. 不覆盖线上 `data.db`、`.env`、`static/images/`。
6. 确认管理员密码通过环境变量 `ADMIN_PASSWORD` 设置，不写进仓库。
7. 同步代码后重启 `8000` 端口服务。

## 部署后验证

部署后需要验证：

```bash
curl -i http://116.142.250.54:8000/
curl -i http://116.142.250.54:8000/admin
curl -i http://116.142.250.54:8000/api/me
```

预期：

- `/` 能打开供应商提交页。
- `/admin` 能打开管理员登录/管理页。
- 未登录访问 `/api/me` 应返回未授权或未登录状态，而不是 `404`。
- 登录后台后能看到“手动创建任务”入口。
- 管理员创建任务时可以选择并发数和是否阶梯式并发。

## 重要提醒

- `129.204.245.50` 不是当前确认的生图压测平台生产入口。
- 如后续需要把当前代码真正部署到生产，应部署到 `116.142.250.54:/opt/stress_test`。
- 服务器凭据、API Key、管理员密码只允许放在安全的本地记录或环境变量中，不允许写入 Git。
